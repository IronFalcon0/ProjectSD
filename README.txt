#SD project1


Informação de instalação:


Commands:
exit()                      -> closes client-server connection
cd server *folderName*      -> changes the current folder on the server side
cd client *folderName*      -> changes the current folder on the client side
ls server                   -> list files and folders on the current server folder
ls client                   -> list files and folders on the current client folder
get *fileName*
send *fileName*
cp *newPassword*